<?php

/**
 * Base class that represents a row from the 'payment' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.2 on:
 *
 * Fri Oct 21 16:12:23 2011
 *
 * @package    lib.model.credit.om
 */
abstract class BasePayment extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        PaymentPeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        string
	 */
	protected $id;

	/**
	 * The value for the credit_id field.
	 * @var        string
	 */
	protected $credit_id;

	/**
	 * The value for the transaction_id field.
	 * @var        string
	 */
	protected $transaction_id;

	/**
	 * The value for the number field.
	 * @var        int
	 */
	protected $number;

	/**
	 * The value for the date field.
	 * @var        string
	 */
	protected $date;

	/**
	 * The value for the balance field.
	 * @var        string
	 */
	protected $balance;

	/**
	 * The value for the capital field.
	 * @var        string
	 */
	protected $capital;

	/**
	 * The value for the interest field.
	 * @var        string
	 */
	protected $interest;

	/**
	 * The value for the status field.
	 * @var        string
	 */
	protected $status;

	/**
	 * The value for the days_in_arrear field.
	 * @var        int
	 */
	protected $days_in_arrear;

	/**
	 * The value for the arrear field.
	 * @var        string
	 */
	protected $arrear;

	/**
	 * The value for the discount field.
	 * @var        string
	 */
	protected $discount;

	/**
	 * @var        Credit
	 */
	protected $aCredit;

	/**
	 * @var        CreditTransaction
	 */
	protected $aTransaction;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'PaymentPeer';

	/**
	 * Get the [id] column value.
	 * 
	 * @return     string
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [credit_id] column value.
	 * 
	 * @return     string
	 */
	public function getCreditId()
	{
		return $this->credit_id;
	}

	/**
	 * Get the [transaction_id] column value.
	 * 
	 * @return     string
	 */
	public function getTransactionId()
	{
		return $this->transaction_id;
	}

	/**
	 * Get the [number] column value.
	 * 
	 * @return     int
	 */
	public function getNumber()
	{
		return $this->number;
	}

	/**
	 * Get the [optionally formatted] temporal [date] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getDate($format = 'Y-m-d')
	{
		if ($this->date === null) {
			return null;
		}


		if ($this->date === '0000-00-00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->date);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->date, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [balance] column value.
	 * 
	 * @return     string
	 */
	public function getBalance()
	{
		return $this->balance;
	}

	/**
	 * Get the [capital] column value.
	 * 
	 * @return     string
	 */
	public function getCapital()
	{
		return $this->capital;
	}

	/**
	 * Get the [interest] column value.
	 * 
	 * @return     string
	 */
	public function getInterest()
	{
		return $this->interest;
	}

	/**
	 * Get the [status] column value.
	 * 
	 * @return     string
	 */
	public function getStatus()
	{
		return $this->status;
	}

	/**
	 * Get the [days_in_arrear] column value.
	 * 
	 * @return     int
	 */
	public function getDaysInArrear()
	{
		return $this->days_in_arrear;
	}

	/**
	 * Get the [arrear] column value.
	 * 
	 * @return     string
	 */
	public function getArrear()
	{
		return $this->arrear;
	}

	/**
	 * Get the [discount] column value.
	 * 
	 * @return     string
	 */
	public function getDiscount()
	{
		return $this->discount;
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      string $v new value
	 * @return     Payment The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = PaymentPeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Set the value of [credit_id] column.
	 * 
	 * @param      string $v new value
	 * @return     Payment The current object (for fluent API support)
	 */
	public function setCreditId($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->credit_id !== $v) {
			$this->credit_id = $v;
			$this->modifiedColumns[] = PaymentPeer::CREDIT_ID;
		}

		if ($this->aCredit !== null && $this->aCredit->getId() !== $v) {
			$this->aCredit = null;
		}

		return $this;
	} // setCreditId()

	/**
	 * Set the value of [transaction_id] column.
	 * 
	 * @param      string $v new value
	 * @return     Payment The current object (for fluent API support)
	 */
	public function setTransactionId($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->transaction_id !== $v) {
			$this->transaction_id = $v;
			$this->modifiedColumns[] = PaymentPeer::TRANSACTION_ID;
		}

		if ($this->aTransaction !== null && $this->aTransaction->getId() !== $v) {
			$this->aTransaction = null;
		}

		return $this;
	} // setTransactionId()

	/**
	 * Set the value of [number] column.
	 * 
	 * @param      int $v new value
	 * @return     Payment The current object (for fluent API support)
	 */
	public function setNumber($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->number !== $v) {
			$this->number = $v;
			$this->modifiedColumns[] = PaymentPeer::NUMBER;
		}

		return $this;
	} // setNumber()

	/**
	 * Sets the value of [date] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Payment The current object (for fluent API support)
	 */
	public function setDate($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->date !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->date !== null && $tmpDt = new DateTime($this->date)) ? $tmpDt->format('Y-m-d') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->date = ($dt ? $dt->format('Y-m-d') : null);
				$this->modifiedColumns[] = PaymentPeer::DATE;
			}
		} // if either are not null

		return $this;
	} // setDate()

	/**
	 * Set the value of [balance] column.
	 * 
	 * @param      string $v new value
	 * @return     Payment The current object (for fluent API support)
	 */
	public function setBalance($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->balance !== $v) {
			$this->balance = $v;
			$this->modifiedColumns[] = PaymentPeer::BALANCE;
		}

		return $this;
	} // setBalance()

	/**
	 * Set the value of [capital] column.
	 * 
	 * @param      string $v new value
	 * @return     Payment The current object (for fluent API support)
	 */
	public function setCapital($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->capital !== $v) {
			$this->capital = $v;
			$this->modifiedColumns[] = PaymentPeer::CAPITAL;
		}

		return $this;
	} // setCapital()

	/**
	 * Set the value of [interest] column.
	 * 
	 * @param      string $v new value
	 * @return     Payment The current object (for fluent API support)
	 */
	public function setInterest($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->interest !== $v) {
			$this->interest = $v;
			$this->modifiedColumns[] = PaymentPeer::INTEREST;
		}

		return $this;
	} // setInterest()

	/**
	 * Set the value of [status] column.
	 * 
	 * @param      string $v new value
	 * @return     Payment The current object (for fluent API support)
	 */
	public function setStatus($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->status !== $v) {
			$this->status = $v;
			$this->modifiedColumns[] = PaymentPeer::STATUS;
		}

		return $this;
	} // setStatus()

	/**
	 * Set the value of [days_in_arrear] column.
	 * 
	 * @param      int $v new value
	 * @return     Payment The current object (for fluent API support)
	 */
	public function setDaysInArrear($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->days_in_arrear !== $v) {
			$this->days_in_arrear = $v;
			$this->modifiedColumns[] = PaymentPeer::DAYS_IN_ARREAR;
		}

		return $this;
	} // setDaysInArrear()

	/**
	 * Set the value of [arrear] column.
	 * 
	 * @param      string $v new value
	 * @return     Payment The current object (for fluent API support)
	 */
	public function setArrear($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->arrear !== $v) {
			$this->arrear = $v;
			$this->modifiedColumns[] = PaymentPeer::ARREAR;
		}

		return $this;
	} // setArrear()

	/**
	 * Set the value of [discount] column.
	 * 
	 * @param      string $v new value
	 * @return     Payment The current object (for fluent API support)
	 */
	public function setDiscount($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->discount !== $v) {
			$this->discount = $v;
			$this->modifiedColumns[] = PaymentPeer::DISCOUNT;
		}

		return $this;
	} // setDiscount()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (string) $row[$startcol + 0] : null;
			$this->credit_id = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->transaction_id = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->number = ($row[$startcol + 3] !== null) ? (int) $row[$startcol + 3] : null;
			$this->date = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->balance = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->capital = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
			$this->interest = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
			$this->status = ($row[$startcol + 8] !== null) ? (string) $row[$startcol + 8] : null;
			$this->days_in_arrear = ($row[$startcol + 9] !== null) ? (int) $row[$startcol + 9] : null;
			$this->arrear = ($row[$startcol + 10] !== null) ? (string) $row[$startcol + 10] : null;
			$this->discount = ($row[$startcol + 11] !== null) ? (string) $row[$startcol + 11] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 12; // 12 = PaymentPeer::NUM_COLUMNS - PaymentPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Payment object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aCredit !== null && $this->credit_id !== $this->aCredit->getId()) {
			$this->aCredit = null;
		}
		if ($this->aTransaction !== null && $this->transaction_id !== $this->aTransaction->getId()) {
			$this->aTransaction = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(PaymentPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = PaymentPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aCredit = null;
			$this->aTransaction = null;
		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(PaymentPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BasePayment:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			
			    return;
			  }
			}

			if ($ret) {
				PaymentPeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BasePayment:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(PaymentPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BasePayment:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    $con->commit();
			
			    return $affectedRows;
			  }
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BasePayment:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				PaymentPeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aCredit !== null) {
				if ($this->aCredit->isModified() || $this->aCredit->isNew()) {
					$affectedRows += $this->aCredit->save($con);
				}
				$this->setCredit($this->aCredit);
			}

			if ($this->aTransaction !== null) {
				if ($this->aTransaction->isModified() || $this->aTransaction->isNew()) {
					$affectedRows += $this->aTransaction->save($con);
				}
				$this->setTransaction($this->aTransaction);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = PaymentPeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = PaymentPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += PaymentPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aCredit !== null) {
				if (!$this->aCredit->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aCredit->getValidationFailures());
				}
			}

			if ($this->aTransaction !== null) {
				if (!$this->aTransaction->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aTransaction->getValidationFailures());
				}
			}


			if (($retval = PaymentPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}



			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = PaymentPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getCreditId();
				break;
			case 2:
				return $this->getTransactionId();
				break;
			case 3:
				return $this->getNumber();
				break;
			case 4:
				return $this->getDate();
				break;
			case 5:
				return $this->getBalance();
				break;
			case 6:
				return $this->getCapital();
				break;
			case 7:
				return $this->getInterest();
				break;
			case 8:
				return $this->getStatus();
				break;
			case 9:
				return $this->getDaysInArrear();
				break;
			case 10:
				return $this->getArrear();
				break;
			case 11:
				return $this->getDiscount();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = PaymentPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getCreditId(),
			$keys[2] => $this->getTransactionId(),
			$keys[3] => $this->getNumber(),
			$keys[4] => $this->getDate(),
			$keys[5] => $this->getBalance(),
			$keys[6] => $this->getCapital(),
			$keys[7] => $this->getInterest(),
			$keys[8] => $this->getStatus(),
			$keys[9] => $this->getDaysInArrear(),
			$keys[10] => $this->getArrear(),
			$keys[11] => $this->getDiscount(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = PaymentPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setCreditId($value);
				break;
			case 2:
				$this->setTransactionId($value);
				break;
			case 3:
				$this->setNumber($value);
				break;
			case 4:
				$this->setDate($value);
				break;
			case 5:
				$this->setBalance($value);
				break;
			case 6:
				$this->setCapital($value);
				break;
			case 7:
				$this->setInterest($value);
				break;
			case 8:
				$this->setStatus($value);
				break;
			case 9:
				$this->setDaysInArrear($value);
				break;
			case 10:
				$this->setArrear($value);
				break;
			case 11:
				$this->setDiscount($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = PaymentPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setCreditId($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setTransactionId($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setNumber($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setDate($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setBalance($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setCapital($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setInterest($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setStatus($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setDaysInArrear($arr[$keys[9]]);
		if (array_key_exists($keys[10], $arr)) $this->setArrear($arr[$keys[10]]);
		if (array_key_exists($keys[11], $arr)) $this->setDiscount($arr[$keys[11]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(PaymentPeer::DATABASE_NAME);

		if ($this->isColumnModified(PaymentPeer::ID)) $criteria->add(PaymentPeer::ID, $this->id);
		if ($this->isColumnModified(PaymentPeer::CREDIT_ID)) $criteria->add(PaymentPeer::CREDIT_ID, $this->credit_id);
		if ($this->isColumnModified(PaymentPeer::TRANSACTION_ID)) $criteria->add(PaymentPeer::TRANSACTION_ID, $this->transaction_id);
		if ($this->isColumnModified(PaymentPeer::NUMBER)) $criteria->add(PaymentPeer::NUMBER, $this->number);
		if ($this->isColumnModified(PaymentPeer::DATE)) $criteria->add(PaymentPeer::DATE, $this->date);
		if ($this->isColumnModified(PaymentPeer::BALANCE)) $criteria->add(PaymentPeer::BALANCE, $this->balance);
		if ($this->isColumnModified(PaymentPeer::CAPITAL)) $criteria->add(PaymentPeer::CAPITAL, $this->capital);
		if ($this->isColumnModified(PaymentPeer::INTEREST)) $criteria->add(PaymentPeer::INTEREST, $this->interest);
		if ($this->isColumnModified(PaymentPeer::STATUS)) $criteria->add(PaymentPeer::STATUS, $this->status);
		if ($this->isColumnModified(PaymentPeer::DAYS_IN_ARREAR)) $criteria->add(PaymentPeer::DAYS_IN_ARREAR, $this->days_in_arrear);
		if ($this->isColumnModified(PaymentPeer::ARREAR)) $criteria->add(PaymentPeer::ARREAR, $this->arrear);
		if ($this->isColumnModified(PaymentPeer::DISCOUNT)) $criteria->add(PaymentPeer::DISCOUNT, $this->discount);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(PaymentPeer::DATABASE_NAME);

		$criteria->add(PaymentPeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     string
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      string $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Payment (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setCreditId($this->credit_id);

		$copyObj->setTransactionId($this->transaction_id);

		$copyObj->setNumber($this->number);

		$copyObj->setDate($this->date);

		$copyObj->setBalance($this->balance);

		$copyObj->setCapital($this->capital);

		$copyObj->setInterest($this->interest);

		$copyObj->setStatus($this->status);

		$copyObj->setDaysInArrear($this->days_in_arrear);

		$copyObj->setArrear($this->arrear);

		$copyObj->setDiscount($this->discount);


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Payment Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     PaymentPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new PaymentPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a Credit object.
	 *
	 * @param      Credit $v
	 * @return     Payment The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setCredit(Credit $v = null)
	{
		if ($v === null) {
			$this->setCreditId(NULL);
		} else {
			$this->setCreditId($v->getId());
		}

		$this->aCredit = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Credit object, it will not be re-added.
		if ($v !== null) {
			$v->addPayment($this);
		}

		return $this;
	}


	/**
	 * Get the associated Credit object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Credit The associated Credit object.
	 * @throws     PropelException
	 */
	public function getCredit(PropelPDO $con = null)
	{
		if ($this->aCredit === null && (($this->credit_id !== "" && $this->credit_id !== null))) {
			$this->aCredit = CreditPeer::retrieveByPk($this->credit_id);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aCredit->addPayments($this);
			 */
		}
		return $this->aCredit;
	}

	/**
	 * Declares an association between this object and a CreditTransaction object.
	 *
	 * @param      CreditTransaction $v
	 * @return     Payment The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setTransaction(CreditTransaction $v = null)
	{
		if ($v === null) {
			$this->setTransactionId(NULL);
		} else {
			$this->setTransactionId($v->getId());
		}

		$this->aTransaction = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the CreditTransaction object, it will not be re-added.
		if ($v !== null) {
			$v->addPayment($this);
		}

		return $this;
	}


	/**
	 * Get the associated CreditTransaction object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     CreditTransaction The associated CreditTransaction object.
	 * @throws     PropelException
	 */
	public function getTransaction(PropelPDO $con = null)
	{
		if ($this->aTransaction === null && (($this->transaction_id !== "" && $this->transaction_id !== null))) {
			$this->aTransaction = CreditTransactionPeer::retrieveByPk($this->transaction_id);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aTransaction->addPayments($this);
			 */
		}
		return $this->aTransaction;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
		} // if ($deep)

			$this->aCredit = null;
			$this->aTransaction = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BasePayment:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BasePayment::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BasePayment
