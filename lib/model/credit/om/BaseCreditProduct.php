<?php

/**
 * Base class that represents a row from the 'credit_product' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.2 on:
 *
 * Tue Jan  8 23:27:28 2013
 *
 * @package    lib.model.credit.om
 */
abstract class BaseCreditProduct extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        CreditProductPeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        string
	 */
	protected $id;

	/**
	 * The value for the name field.
	 * @var        string
	 */
	protected $name;

	/**
	 * The value for the amortization_type field.
	 * Note: this column has a database default value of: 'german'
	 * @var        string
	 */
	protected $amortization_type;

	/**
	 * The value for the grace_days field.
	 * Note: this column has a database default value of: 0
	 * @var        int
	 */
	protected $grace_days;

	/**
	 * The value for the created_at field.
	 * @var        string
	 */
	protected $created_at;

	/**
	 * The value for the updated_at field.
	 * @var        string
	 */
	protected $updated_at;

	/**
	 * @var        array CreditProductInterestRate[] Collection to store aggregation of CreditProductInterestRate objects.
	 */
	protected $collCreditProductInterestRates;

	/**
	 * @var        Criteria The criteria used to select the current contents of collCreditProductInterestRates.
	 */
	private $lastCreditProductInterestRateCriteria = null;

	/**
	 * @var        array CreditProductArrearRate[] Collection to store aggregation of CreditProductArrearRate objects.
	 */
	protected $collCreditProductArrearRates;

	/**
	 * @var        Criteria The criteria used to select the current contents of collCreditProductArrearRates.
	 */
	private $lastCreditProductArrearRateCriteria = null;

	/**
	 * @var        array Credit[] Collection to store aggregation of Credit objects.
	 */
	protected $collCredits;

	/**
	 * @var        Criteria The criteria used to select the current contents of collCredits.
	 */
	private $lastCreditCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'CreditProductPeer';

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
		$this->amortization_type = 'german';
		$this->grace_days = 0;
	}

	/**
	 * Initializes internal state of BaseCreditProduct object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Get the [id] column value.
	 * 
	 * @return     string
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [name] column value.
	 * 
	 * @return     string
	 */
	public function getName()
	{
		return $this->name;
	}

	/**
	 * Get the [amortization_type] column value.
	 * 
	 * @return     string
	 */
	public function getAmortizationType()
	{
		return $this->amortization_type;
	}

	/**
	 * Get the [grace_days] column value.
	 * 
	 * @return     int
	 */
	public function getGraceDays()
	{
		return $this->grace_days;
	}

	/**
	 * Get the [optionally formatted] temporal [created_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getCreatedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->created_at === null) {
			return null;
		}


		if ($this->created_at === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->created_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->created_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [updated_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getUpdatedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->updated_at === null) {
			return null;
		}


		if ($this->updated_at === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->updated_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->updated_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      string $v new value
	 * @return     CreditProduct The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = CreditProductPeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Set the value of [name] column.
	 * 
	 * @param      string $v new value
	 * @return     CreditProduct The current object (for fluent API support)
	 */
	public function setName($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->name !== $v) {
			$this->name = $v;
			$this->modifiedColumns[] = CreditProductPeer::NAME;
		}

		return $this;
	} // setName()

	/**
	 * Set the value of [amortization_type] column.
	 * 
	 * @param      string $v new value
	 * @return     CreditProduct The current object (for fluent API support)
	 */
	public function setAmortizationType($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->amortization_type !== $v || $this->isNew()) {
			$this->amortization_type = $v;
			$this->modifiedColumns[] = CreditProductPeer::AMORTIZATION_TYPE;
		}

		return $this;
	} // setAmortizationType()

	/**
	 * Set the value of [grace_days] column.
	 * 
	 * @param      int $v new value
	 * @return     CreditProduct The current object (for fluent API support)
	 */
	public function setGraceDays($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->grace_days !== $v || $this->isNew()) {
			$this->grace_days = $v;
			$this->modifiedColumns[] = CreditProductPeer::GRACE_DAYS;
		}

		return $this;
	} // setGraceDays()

	/**
	 * Sets the value of [created_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     CreditProduct The current object (for fluent API support)
	 */
	public function setCreatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->created_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->created_at !== null && $tmpDt = new DateTime($this->created_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->created_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = CreditProductPeer::CREATED_AT;
			}
		} // if either are not null

		return $this;
	} // setCreatedAt()

	/**
	 * Sets the value of [updated_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     CreditProduct The current object (for fluent API support)
	 */
	public function setUpdatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->updated_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->updated_at !== null && $tmpDt = new DateTime($this->updated_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->updated_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = CreditProductPeer::UPDATED_AT;
			}
		} // if either are not null

		return $this;
	} // setUpdatedAt()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			if ($this->amortization_type !== 'german') {
				return false;
			}

			if ($this->grace_days !== 0) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (string) $row[$startcol + 0] : null;
			$this->name = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->amortization_type = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->grace_days = ($row[$startcol + 3] !== null) ? (int) $row[$startcol + 3] : null;
			$this->created_at = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->updated_at = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 6; // 6 = CreditProductPeer::NUM_COLUMNS - CreditProductPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating CreditProduct object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(CreditProductPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = CreditProductPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->collCreditProductInterestRates = null;
			$this->lastCreditProductInterestRateCriteria = null;

			$this->collCreditProductArrearRates = null;
			$this->lastCreditProductArrearRateCriteria = null;

			$this->collCredits = null;
			$this->lastCreditCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(CreditProductPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseCreditProduct:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			
			    return;
			  }
			}

			if ($ret) {
				CreditProductPeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseCreditProduct:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(CreditProductPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseCreditProduct:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    $con->commit();
			
			    return $affectedRows;
			  }
			}

			// symfony_timestampable behavior
			if ($this->isModified() && !$this->isColumnModified(CreditProductPeer::UPDATED_AT))
			{
			  $this->setUpdatedAt(time());
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
				// symfony_timestampable behavior
				if (!$this->isColumnModified(CreditProductPeer::CREATED_AT))
				{
				  $this->setCreatedAt(time());
				}

			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseCreditProduct:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				CreditProductPeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			if ($this->isNew() ) {
				$this->modifiedColumns[] = CreditProductPeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = CreditProductPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += CreditProductPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collCreditProductInterestRates !== null) {
				foreach ($this->collCreditProductInterestRates as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collCreditProductArrearRates !== null) {
				foreach ($this->collCreditProductArrearRates as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collCredits !== null) {
				foreach ($this->collCredits as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			if (($retval = CreditProductPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collCreditProductInterestRates !== null) {
					foreach ($this->collCreditProductInterestRates as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collCreditProductArrearRates !== null) {
					foreach ($this->collCreditProductArrearRates as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collCredits !== null) {
					foreach ($this->collCredits as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = CreditProductPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getName();
				break;
			case 2:
				return $this->getAmortizationType();
				break;
			case 3:
				return $this->getGraceDays();
				break;
			case 4:
				return $this->getCreatedAt();
				break;
			case 5:
				return $this->getUpdatedAt();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = CreditProductPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getName(),
			$keys[2] => $this->getAmortizationType(),
			$keys[3] => $this->getGraceDays(),
			$keys[4] => $this->getCreatedAt(),
			$keys[5] => $this->getUpdatedAt(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = CreditProductPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setName($value);
				break;
			case 2:
				$this->setAmortizationType($value);
				break;
			case 3:
				$this->setGraceDays($value);
				break;
			case 4:
				$this->setCreatedAt($value);
				break;
			case 5:
				$this->setUpdatedAt($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = CreditProductPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setName($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setAmortizationType($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setGraceDays($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setCreatedAt($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setUpdatedAt($arr[$keys[5]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(CreditProductPeer::DATABASE_NAME);

		if ($this->isColumnModified(CreditProductPeer::ID)) $criteria->add(CreditProductPeer::ID, $this->id);
		if ($this->isColumnModified(CreditProductPeer::NAME)) $criteria->add(CreditProductPeer::NAME, $this->name);
		if ($this->isColumnModified(CreditProductPeer::AMORTIZATION_TYPE)) $criteria->add(CreditProductPeer::AMORTIZATION_TYPE, $this->amortization_type);
		if ($this->isColumnModified(CreditProductPeer::GRACE_DAYS)) $criteria->add(CreditProductPeer::GRACE_DAYS, $this->grace_days);
		if ($this->isColumnModified(CreditProductPeer::CREATED_AT)) $criteria->add(CreditProductPeer::CREATED_AT, $this->created_at);
		if ($this->isColumnModified(CreditProductPeer::UPDATED_AT)) $criteria->add(CreditProductPeer::UPDATED_AT, $this->updated_at);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(CreditProductPeer::DATABASE_NAME);

		$criteria->add(CreditProductPeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     string
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      string $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of CreditProduct (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setName($this->name);

		$copyObj->setAmortizationType($this->amortization_type);

		$copyObj->setGraceDays($this->grace_days);

		$copyObj->setCreatedAt($this->created_at);

		$copyObj->setUpdatedAt($this->updated_at);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getCreditProductInterestRates() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addCreditProductInterestRate($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getCreditProductArrearRates() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addCreditProductArrearRate($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getCredits() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addCredit($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     CreditProduct Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     CreditProductPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new CreditProductPeer();
		}
		return self::$peer;
	}

	/**
	 * Clears out the collCreditProductInterestRates collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addCreditProductInterestRates()
	 */
	public function clearCreditProductInterestRates()
	{
		$this->collCreditProductInterestRates = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collCreditProductInterestRates collection (array).
	 *
	 * By default this just sets the collCreditProductInterestRates collection to an empty array (like clearcollCreditProductInterestRates());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initCreditProductInterestRates()
	{
		$this->collCreditProductInterestRates = array();
	}

	/**
	 * Gets an array of CreditProductInterestRate objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this CreditProduct has previously been saved, it will retrieve
	 * related CreditProductInterestRates from storage. If this CreditProduct is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array CreditProductInterestRate[]
	 * @throws     PropelException
	 */
	public function getCreditProductInterestRates($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CreditProductPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCreditProductInterestRates === null) {
			if ($this->isNew()) {
			   $this->collCreditProductInterestRates = array();
			} else {

				$criteria->add(CreditProductInterestRatePeer::PRODUCT_ID, $this->id);

				CreditProductInterestRatePeer::addSelectColumns($criteria);
				$this->collCreditProductInterestRates = CreditProductInterestRatePeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(CreditProductInterestRatePeer::PRODUCT_ID, $this->id);

				CreditProductInterestRatePeer::addSelectColumns($criteria);
				if (!isset($this->lastCreditProductInterestRateCriteria) || !$this->lastCreditProductInterestRateCriteria->equals($criteria)) {
					$this->collCreditProductInterestRates = CreditProductInterestRatePeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastCreditProductInterestRateCriteria = $criteria;
		return $this->collCreditProductInterestRates;
	}

	/**
	 * Returns the number of related CreditProductInterestRate objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related CreditProductInterestRate objects.
	 * @throws     PropelException
	 */
	public function countCreditProductInterestRates(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CreditProductPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collCreditProductInterestRates === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(CreditProductInterestRatePeer::PRODUCT_ID, $this->id);

				$count = CreditProductInterestRatePeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(CreditProductInterestRatePeer::PRODUCT_ID, $this->id);

				if (!isset($this->lastCreditProductInterestRateCriteria) || !$this->lastCreditProductInterestRateCriteria->equals($criteria)) {
					$count = CreditProductInterestRatePeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collCreditProductInterestRates);
				}
			} else {
				$count = count($this->collCreditProductInterestRates);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a CreditProductInterestRate object to this object
	 * through the CreditProductInterestRate foreign key attribute.
	 *
	 * @param      CreditProductInterestRate $l CreditProductInterestRate
	 * @return     void
	 * @throws     PropelException
	 */
	public function addCreditProductInterestRate(CreditProductInterestRate $l)
	{
		if ($this->collCreditProductInterestRates === null) {
			$this->initCreditProductInterestRates();
		}
		if (!in_array($l, $this->collCreditProductInterestRates, true)) { // only add it if the **same** object is not already associated
			array_push($this->collCreditProductInterestRates, $l);
			$l->setProduct($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CreditProduct is new, it will return
	 * an empty collection; or if this CreditProduct has previously
	 * been saved, it will retrieve related CreditProductInterestRates from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CreditProduct.
	 */
	public function getCreditProductInterestRatesJoinRateUnique($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CreditProductPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCreditProductInterestRates === null) {
			if ($this->isNew()) {
				$this->collCreditProductInterestRates = array();
			} else {

				$criteria->add(CreditProductInterestRatePeer::PRODUCT_ID, $this->id);

				$this->collCreditProductInterestRates = CreditProductInterestRatePeer::doSelectJoinRateUnique($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CreditProductInterestRatePeer::PRODUCT_ID, $this->id);

			if (!isset($this->lastCreditProductInterestRateCriteria) || !$this->lastCreditProductInterestRateCriteria->equals($criteria)) {
				$this->collCreditProductInterestRates = CreditProductInterestRatePeer::doSelectJoinRateUnique($criteria, $con, $join_behavior);
			}
		}
		$this->lastCreditProductInterestRateCriteria = $criteria;

		return $this->collCreditProductInterestRates;
	}

	/**
	 * Clears out the collCreditProductArrearRates collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addCreditProductArrearRates()
	 */
	public function clearCreditProductArrearRates()
	{
		$this->collCreditProductArrearRates = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collCreditProductArrearRates collection (array).
	 *
	 * By default this just sets the collCreditProductArrearRates collection to an empty array (like clearcollCreditProductArrearRates());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initCreditProductArrearRates()
	{
		$this->collCreditProductArrearRates = array();
	}

	/**
	 * Gets an array of CreditProductArrearRate objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this CreditProduct has previously been saved, it will retrieve
	 * related CreditProductArrearRates from storage. If this CreditProduct is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array CreditProductArrearRate[]
	 * @throws     PropelException
	 */
	public function getCreditProductArrearRates($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CreditProductPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCreditProductArrearRates === null) {
			if ($this->isNew()) {
			   $this->collCreditProductArrearRates = array();
			} else {

				$criteria->add(CreditProductArrearRatePeer::PRODUCT_ID, $this->id);

				CreditProductArrearRatePeer::addSelectColumns($criteria);
				$this->collCreditProductArrearRates = CreditProductArrearRatePeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(CreditProductArrearRatePeer::PRODUCT_ID, $this->id);

				CreditProductArrearRatePeer::addSelectColumns($criteria);
				if (!isset($this->lastCreditProductArrearRateCriteria) || !$this->lastCreditProductArrearRateCriteria->equals($criteria)) {
					$this->collCreditProductArrearRates = CreditProductArrearRatePeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastCreditProductArrearRateCriteria = $criteria;
		return $this->collCreditProductArrearRates;
	}

	/**
	 * Returns the number of related CreditProductArrearRate objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related CreditProductArrearRate objects.
	 * @throws     PropelException
	 */
	public function countCreditProductArrearRates(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CreditProductPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collCreditProductArrearRates === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(CreditProductArrearRatePeer::PRODUCT_ID, $this->id);

				$count = CreditProductArrearRatePeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(CreditProductArrearRatePeer::PRODUCT_ID, $this->id);

				if (!isset($this->lastCreditProductArrearRateCriteria) || !$this->lastCreditProductArrearRateCriteria->equals($criteria)) {
					$count = CreditProductArrearRatePeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collCreditProductArrearRates);
				}
			} else {
				$count = count($this->collCreditProductArrearRates);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a CreditProductArrearRate object to this object
	 * through the CreditProductArrearRate foreign key attribute.
	 *
	 * @param      CreditProductArrearRate $l CreditProductArrearRate
	 * @return     void
	 * @throws     PropelException
	 */
	public function addCreditProductArrearRate(CreditProductArrearRate $l)
	{
		if ($this->collCreditProductArrearRates === null) {
			$this->initCreditProductArrearRates();
		}
		if (!in_array($l, $this->collCreditProductArrearRates, true)) { // only add it if the **same** object is not already associated
			array_push($this->collCreditProductArrearRates, $l);
			$l->setProduct($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CreditProduct is new, it will return
	 * an empty collection; or if this CreditProduct has previously
	 * been saved, it will retrieve related CreditProductArrearRates from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CreditProduct.
	 */
	public function getCreditProductArrearRatesJoinRateUnique($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CreditProductPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCreditProductArrearRates === null) {
			if ($this->isNew()) {
				$this->collCreditProductArrearRates = array();
			} else {

				$criteria->add(CreditProductArrearRatePeer::PRODUCT_ID, $this->id);

				$this->collCreditProductArrearRates = CreditProductArrearRatePeer::doSelectJoinRateUnique($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CreditProductArrearRatePeer::PRODUCT_ID, $this->id);

			if (!isset($this->lastCreditProductArrearRateCriteria) || !$this->lastCreditProductArrearRateCriteria->equals($criteria)) {
				$this->collCreditProductArrearRates = CreditProductArrearRatePeer::doSelectJoinRateUnique($criteria, $con, $join_behavior);
			}
		}
		$this->lastCreditProductArrearRateCriteria = $criteria;

		return $this->collCreditProductArrearRates;
	}

	/**
	 * Clears out the collCredits collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addCredits()
	 */
	public function clearCredits()
	{
		$this->collCredits = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collCredits collection (array).
	 *
	 * By default this just sets the collCredits collection to an empty array (like clearcollCredits());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initCredits()
	{
		$this->collCredits = array();
	}

	/**
	 * Gets an array of Credit objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this CreditProduct has previously been saved, it will retrieve
	 * related Credits from storage. If this CreditProduct is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Credit[]
	 * @throws     PropelException
	 */
	public function getCredits($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CreditProductPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCredits === null) {
			if ($this->isNew()) {
			   $this->collCredits = array();
			} else {

				$criteria->add(CreditPeer::PRODUCT_ID, $this->id);

				CreditPeer::addSelectColumns($criteria);
				$this->collCredits = CreditPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(CreditPeer::PRODUCT_ID, $this->id);

				CreditPeer::addSelectColumns($criteria);
				if (!isset($this->lastCreditCriteria) || !$this->lastCreditCriteria->equals($criteria)) {
					$this->collCredits = CreditPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastCreditCriteria = $criteria;
		return $this->collCredits;
	}

	/**
	 * Returns the number of related Credit objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Credit objects.
	 * @throws     PropelException
	 */
	public function countCredits(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CreditProductPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collCredits === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(CreditPeer::PRODUCT_ID, $this->id);

				$count = CreditPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(CreditPeer::PRODUCT_ID, $this->id);

				if (!isset($this->lastCreditCriteria) || !$this->lastCreditCriteria->equals($criteria)) {
					$count = CreditPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collCredits);
				}
			} else {
				$count = count($this->collCredits);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Credit object to this object
	 * through the Credit foreign key attribute.
	 *
	 * @param      Credit $l Credit
	 * @return     void
	 * @throws     PropelException
	 */
	public function addCredit(Credit $l)
	{
		if ($this->collCredits === null) {
			$this->initCredits();
		}
		if (!in_array($l, $this->collCredits, true)) { // only add it if the **same** object is not already associated
			array_push($this->collCredits, $l);
			$l->setProduct($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CreditProduct is new, it will return
	 * an empty collection; or if this CreditProduct has previously
	 * been saved, it will retrieve related Credits from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CreditProduct.
	 */
	public function getCreditsJoinAssociate($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CreditProductPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCredits === null) {
			if ($this->isNew()) {
				$this->collCredits = array();
			} else {

				$criteria->add(CreditPeer::PRODUCT_ID, $this->id);

				$this->collCredits = CreditPeer::doSelectJoinAssociate($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CreditPeer::PRODUCT_ID, $this->id);

			if (!isset($this->lastCreditCriteria) || !$this->lastCreditCriteria->equals($criteria)) {
				$this->collCredits = CreditPeer::doSelectJoinAssociate($criteria, $con, $join_behavior);
			}
		}
		$this->lastCreditCriteria = $criteria;

		return $this->collCredits;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CreditProduct is new, it will return
	 * an empty collection; or if this CreditProduct has previously
	 * been saved, it will retrieve related Credits from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CreditProduct.
	 */
	public function getCreditsJoinAccount($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CreditProductPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCredits === null) {
			if ($this->isNew()) {
				$this->collCredits = array();
			} else {

				$criteria->add(CreditPeer::PRODUCT_ID, $this->id);

				$this->collCredits = CreditPeer::doSelectJoinAccount($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CreditPeer::PRODUCT_ID, $this->id);

			if (!isset($this->lastCreditCriteria) || !$this->lastCreditCriteria->equals($criteria)) {
				$this->collCredits = CreditPeer::doSelectJoinAccount($criteria, $con, $join_behavior);
			}
		}
		$this->lastCreditCriteria = $criteria;

		return $this->collCredits;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collCreditProductInterestRates) {
				foreach ((array) $this->collCreditProductInterestRates as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collCreditProductArrearRates) {
				foreach ((array) $this->collCreditProductArrearRates as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collCredits) {
				foreach ((array) $this->collCredits as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collCreditProductInterestRates = null;
		$this->collCreditProductArrearRates = null;
		$this->collCredits = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseCreditProduct:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseCreditProduct::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseCreditProduct
